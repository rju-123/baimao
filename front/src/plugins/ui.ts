import type { App } from 'vue';
/**
 * NutUI-Uniapp 通过 pages.json easycom 按需引入，无需 app.use
 * 主题与全局样式见 uni.scss、theme.scss
 */
export default function setupUI(_app: App) {
  // 若需全局配置 NutUI 主题，可在此扩展
}
