# 复制为 .env.development / .env.production 等（勿提交含密钥的文件）
#
# 【微信小程序体验版 / 正式版真机】请求后端必须满足：
# 1. VITE_API_BASE_URL 使用「已备案域名 + HTTPS」，例如 https://api.example.com
#    （不能使用 http://、不能使用裸 IP 作为 request 合法域名）
# 2. 在微信公众平台 → 开发 → 开发管理 → 开发设置 → 服务器域名
#    「request 合法域名」填写与上面一致的主机名（不要带路径、不要带端口 443）
# 3. 开发者工具里「不校验合法域名」仅对工具生效，真机与体验版仍会拦截未配置域名
#
VITE_APP_ENV=development
VITE_API_BASE_URL=https://your-api-domain.example.com
