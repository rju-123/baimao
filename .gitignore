# Dependencies
node_modules/

# Build / dist
dist/
unpackage/
*.local

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# IDE
.idea/
*.suo
*.ntvs*
.DS_Store

# Env (keep .env.example if any)
.env.local
.env.*.local
api/.env

# SQLite (optional: track or ignore)
# *.sqlite
# *.db

# Uploads (local files)
api/uploads/
admin/public/uploads/

# FastAdmin / ThinkPHP
admin/.env
admin/runtime/
admin/application/admin/command/Install/install.lock

# SQLite (local dev DB)
api/database.sqlite

# Local archives / full DB dumps (do not commit)
*.zip
/baimao_admin.sql

# FastAdmin install artifacts (do not commit)
admin/public/install.php
admin/public/check_lock.php

# Lock files (optional: uncomment to use single package manager)
# package-lock.json
# yarn.lock
